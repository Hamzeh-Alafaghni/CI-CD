// Default (development) environment.
// `ng build` swaps this file for the staging/production variant
// via the fileReplacements in angular.json.
export const environment = {
  production: false,
  name: 'development',
  apiUrl: 'http://localhost:5080',
};
